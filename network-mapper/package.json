{
  "name": "network-mapper",
  "version": "1.0.0",
  "description": "A small, HTML-first web app to map school buildings, floorplans and network devices (switches, MDF/IDF, phones, APs, cameras). Dockerized Flask backend (SQLite) + static frontend.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
